{
  "extends": ["plugin:jest/all", "standard", "prettier-standard"],
  "plugins": ["jest"],
  "rules": {
    "jest/lowercase-name": [
      "error",
      {
        "allowedPrefixes": ["GET", "POST", "PATCH", "PUT", "DELETE", "OPTIONS"]
      }
    ],
    "jest/no-hooks": 0
  }
}
